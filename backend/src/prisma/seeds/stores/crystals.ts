import type { StoreSeed } from "../types";

export const crystalsStore: StoreSeed = {
    name: "Crystals",
    description: "The main currency for buying exclusive items in the store!",
    priority: 0
};
